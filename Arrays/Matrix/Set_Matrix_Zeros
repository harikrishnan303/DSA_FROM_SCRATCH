Assume firstrow and firstcol as the extra space to change the respective row and columns to zero occurding to the problems statement

first traverse through each and col starting from index 1 and 1

if there is a zero found change the respective firstrow and col index to zeroafter successful traversal move to the next step

traverse throught the matrix if the particular row or col of first ontains zero change the entire to zero
class Solution {
    public void setZeroes(int[][] matrix)
      {
        boolean firstrow=false;
        boolean firstcol=false;
        int m=matrix.length;
        int n=matrix[0].length;
        for(int i=0;i<n;i++)
        {
            if(matrix[0][i]==0)
             firstrow=true;
      }
        for(int i=0;i<m;i++)
        {
            if(matrix[i][0]==0)
             firstcol=true;
        }
        for(int i=0;i<m;i++)
        {
            for(int j=0;j<n;j++)
            {
                if(matrix[i][j]==0)
                {
                    matrix[i][0]=0;
                    matrix[0][j]=0;
                }
            }
        }
        for(int i=1;i<m;i++)
        {
            for(int j=1;j<n;j++)
            {
                if(matrix[i][0]==0||matrix[0][j]==0)
                {
                    matrix[i][j]=0;
                }
            }
        }
        if(firstrow==true)
        {
            for(int i=0;i<n;i++)
            matrix[0][i]=0;
            
        }
        if(firstcol==true)
        {
            for(int i=0;i<m;i++)
            matrix[i][0]=0;
        }
        
    }
}


Time Complexity:O(n^2)
Space Complexity:O(1)