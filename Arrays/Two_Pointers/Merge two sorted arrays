First find the size of the resultant array.

Next using an while loop traverse from the end of both array
compare the values of both the array and insert the maximum into the 
resultant array

class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n)
    {
        int second=n-1;
        int first=m-1;
        int total=m+n-1;

        while(second>=0)
        {
            if(first>=0&&nums1[first]>=nums2[second])
            {
                nums1[total]=nums1[first];
                first--;
                total--;
            }
            else
            {
                nums1[total]=nums2[second];
                second--;
                total--;
            }
        }  
    }
}


Time Complexity:O(N)
Space Complexity:O(1)