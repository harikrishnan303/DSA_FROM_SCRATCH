Insertion sort is also implemented using two loops.Outer loop iterate from the index 1 to n-1 index.
the inner loop traverse back of the outer index's current index 



    for(int i=1;i<n-1;i++)
    {
        int j=i;
        while(j>0&&nums[j]<nums[j-1])
        {
            int temp=nums[j];
            nums[j]=nums[j-1];
            nums[j-1]=temp;
            j--;
        }
    }
