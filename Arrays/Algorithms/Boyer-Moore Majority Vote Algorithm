Boyer-Moore Majority Vote Algorithm

Use the count and elements

if the count goes to zero change the element value to current value

This works as because the the most repeated element appears more than half of the count of elements present in the array

class Solution {
    public int majorityElement(int[] nums) 
    {
        int count=0;
        int element=-1;

        for(int i=0;i<nums.length;i++)
        {
            if(count==0)
            {
                count++;
                element=nums[i];
            }
            else if(element==nums[i])
            {
                count++;
            }
            else 
            count--;
        }
        return element;
        
    }
}